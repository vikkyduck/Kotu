#!/bin/bash
# Остаток Replit (.replit → [postMerge]): после слияния веток ставит зависимости.
set -e
pnpm install --frozen-lockfile
# Схему базы здесь больше НЕ применяем (раньше тут был `pnpm --filter db push`).
# push без дампа может сделать DROP COLUMN или TRUNCATE — мимо архива строк
# (решение владелицы 23.09.2026: стирать нельзя ничего), а до рестарта
# сервера правки пересозданной таблицы не архивируются вовсе. Схема меняется
# только вручную, по DEPLOY.md («Миграция схемы»): дамп → push → рестарт.
